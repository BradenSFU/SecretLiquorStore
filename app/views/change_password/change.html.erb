<h1>Change Password<h2>
<%= form_for @user = :user_params do %>
<div class="row">
  <div class="col-md-3 col-md-offset-3">
    <label for="Username">Username</label> <%= text_field_tag :Username, params[:Username] %>
    <label for="Old Password">Old Password</label> <%= text_field_tag :password, params[:old_password] %>
    <label for="Password">New Password</label> <%= text_field_tag :password, params[:password] %>
    <label for="Password Confirmation">Confirm New Password</label> <%= text_field_tag :password, params[:password_confirmation] %>
    <div class = "actions"> <%= submit_tag "Submit" %> </div>
  </div>
</div>
<%end%>
